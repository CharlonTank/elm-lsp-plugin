---
description: Format an Elm file using elm-format
arguments:
  - name: file
    description: The Elm file to format (e.g., "src/Module.elm")
---

# Elm Format

Format `$ARGUMENTS.file` using elm-format.

## Instructions

1. Use `mcp__elm-lsp__elm_format` with the file path

2. Report whether the file was modified or was already properly formatted
